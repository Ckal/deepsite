name: deepsite
type: gradio-space
sdk: docker

metadata:
  space_id: Chris4K/deepsite
  original_url: https://huggingface.co/spaces/Chris4K/deepsite

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"
